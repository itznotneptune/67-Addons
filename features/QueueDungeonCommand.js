import config from "../config"

register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_SEVEN")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm7")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_SIX")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm6")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_FIVE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm5")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_FOUR")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm4")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_THREE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm3")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_TWO")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm2")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon MASTER_CATACOMBS_FLOOR_ONE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: mm1")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_SEVEN")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff7")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_SIX")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff6")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_FIVE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff5")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_FOUR")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff4")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_THREE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff3")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_TWO")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff2")
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_FLOOR_ONE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff1")
    //Making a command for entrance cause why not
    register("chat", () => {
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("pc Joining Instance! Equip!")
    }, 300)
    if (config.dungeonCmd) setTimeout(() => {
        ChatLib.command("joindungeon CATACOMBS_ENTRANCE")
    }, 400)
    }).setCriteria("Party > ${rank} ${player}: ff0")






